# Synthetic Import Fixture

This export root is hand-authored synthetic test data.

It exists so EditMode smoke tests and future CI jobs can exercise the
Gen5 validation/import seam without depending on a local ROM or ROM-derived
content exports.
